services:
  backend:
    build: ./backend
    container_name: sivag_backend
    ports:
      - "8001:8000"
    volumes:
      - ./backend:/app
    environment:
      - DB_HOST=host.docker.internal
      - DB_PASSWORD=GreenHat_MY77

      # ── Email (descomentar en producción) ──────────────
      # - EMAIL_HOST=smtp.gmail.com
      # - EMAIL_PORT=587
      # - EMAIL_HOST_USER=no-reply@sivag.ccgs.mx
      # - EMAIL_HOST_PASSWORD=abcdefghijklmnop
      # - FRONTEND_URL=https://sivag.ccgs.mx

    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

networks:
  default:
    name: sivag-net